version: '3.8'

services:
  trading-app:
    build: .
    container_name: trading-prediction
    ports:
      - "5000:5000"
    environment:
      - HOST=0.0.0.0
      - PORT=5000
    volumes:
      - ./data:/app/data
    restart: unless-stopped
